# Tier-2 Ambiguities — d.40 Divisio

Per CLAUDE.md `[?]` logging convention. One entry per inline `[?]` in `vol1/bon-sent-I-d40-divisio.md`.

## Open

**bon-sent-I-d40-divisio, Tractatio quaestionum, second question of De entitate (p. 702 col. 1):**
The OCR (raw line 28265 of pt2) shows the listing cut off as `Secundo, quid sit praedestinatio secundum` with the next OCR column resuming at `ARTICULUS I.` Per the existing sibling chunk `bon-sent-I-d40-a1-q2.md`, this question is *Utrum praedestinatio dicat aliquid in Deo, an in praedestinato* — but the wave-1 dispatch note hints "scientia/voluntas" framing. The truncation prevents a verbatim transcription of the closing word(s) (likely *creatum, an aliquid in Deo* or similar). Currently rendered with `[?]` placeholder. → Resolve at decade-polish from 600 dpi PDF (printed p. 702, top of right column).

**bon-sent-I-d40-divisio, divisio body (p. 701 col. 2):**
OCR shows `cuius est causa, sive quis ' sit eius effectus` — the stray `'` between *quis* and *sit* could be either an OCR-misread comma or a footnote anchor. There is no apparatus block on p. 701 of the Quaracchi printed page (the apparatus at the foot of p. 702 belongs to a1-q1), so a footnote anchor here would be unmatched. Currently rendered without anchor (treating `'` as OCR noise). → Resolve at decade-polish from 600 dpi PDF.
