# Tier-2 ambiguities — bon-sent-I-d7-a1-q4

Per-chunk log of `[?]` flags placed during the 2026-05-11 rebuild from raw OCR (lines 30468-30738).

## Flags

1. **Scholion III, citation `S. Thom., de Potentia q. 2. a. 5[?]`** — raw OCR at line 30732 reads `de Potentia q. 2. a. S. aliter loquitur`. The `S.` is almost certainly a numeral garble; cross-referencing standard Aquinas citation conventions, the De Potentia disputation q. 2 on divine generation/begetting runs articles 1-6 and the relevant article on omnipotence-and-generation is generally a. 5 (or possibly a. 4 or 6). Provisionally rendered as `a. 5[?]`. → Resolve via 600dpi PDF of printed p.144 footer to confirm the numeral.

## Anchor positions (less certain)

The OCR places footnote markers with idiosyncratic glyph substitutions (`'`, `■`, `°`, `«`, `»`, `*`, `"`). The anchor positions chosen in this rebuild follow the OCR superscript-spacing convention but are not all individually verified against the printed page. Specifically uncertain:

- `[^7]` ("Vat. cum cod. cc... *dicit*") — anchored at "nihil dico" because footer #2 of p.143 documents `dicit` variant; OCR shows `nihii dico'` with a marker after `dico`.
- `[^8]` ("Fide mss. et ed. 1 expunximus hic additum *in*") — anchored at "differens solum" because footer documents the expunction of *in* after *solum*; OCR shows `solum ^` superscript.
- `[^11]` ("Vat. praemittit *est*") — anchored at "potest esse" because the variant adds *est* before *potest*.
- `[^13]` ("accomodata") — anchored at "distributio accommoda" matching the variant.

If a future 600dpi audit pass disagrees with any anchor position, move the marker; the apparatus entry contents are verbatim from OCR and stable.

## No-flag verification

- Body Latin transcribed verbatim from raw OCR lines 30468-30628.
- Apparatus rebuilt from raw OCR footers: 5 entries on p.142 (Q.IV-portion, footers 5-9 of p.142 — footers 1-4 of p.142 were taken by the q.3 chunk per its own status string) + 10 entries on p.143 (all 10 of p.143's footers) = 15 total.
- Scholion sections I, II, III, IV transcribed verbatim from OCR lines 30634-30736.
- The 3 fabricated cross-refs from the prior chunk state (Cfr. supra hic q. 3, Distinctio fruibile/utibile ex August., Cfr. supra d. 1 dub. 5 duplex analogia) DELETED — none traced to any raw OCR footer entry.
