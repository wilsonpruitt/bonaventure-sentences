# Tier-2 ambiguities — bon-sent-I-d39-a1-q2

OCR raw lines 26936–27149 of `raw/bonaventure_vol1_pt2_raw.txt`. Two-column page layout heavily garbled the bottom-of-page-690 *Scholion*; the body text and apparatus are clean. All `[?]` flags below are inline in the chunk; resolution requires 600-dpi PDF eyes-on of pp. 689 (left col bottom + right col bottom) and 690 (left col bottom + right col, the SCHOLION block).

## Body

**`*Ad 4.*` — `Ad illud quod quaeritur[?]`**: OCR has `.\d illud t|uod qnaeritur'.` at line 27094. The opening of the reply to obj. 4 reads cleanly enough (`Ad illud quod quaeritur, quae necessitas est…`); the `[?]` marks the `'` apostrophe-mark — likely either an apparatus marker (no apparatus entry corresponds) or OCR garble. Resolve: confirm whether printed text has a footnote anchor here.

## Apparatus

**`[^9]` — `c. li. 20. 24. (18. 21. 31.)`**: OCR `c. li. 20.` likely garble for `c. 31.` or `c. 11.` — the parenthetical `(18. 21. 31.)` are alt chapter numbers. Body marker references Aristotle's *Posterior Analytics* on universals/sense; standard locus is *Post. An.* II.19 (100a). Currently rendered with `[?]`; resolve via PDF.

**`[^12]` — placeholder**: OCR shows a marker (`auctoritatibus`) but no clear apparatus entry text in the OCR; likely the marker is just structural (introduces the two scriptural citations [^13]–[^14]). Currently rendered as bracket-note `[?]`. Resolve: confirm whether printed apparatus has a `12` entry or whether the body marker should be deleted.

**`[^15]` — `Quae litteruntur[?] in 6. arg. ad opposit.`**: OCR `Quae lilleruntur` likely for `Quae allegantur` ("which are alleged") or `Quae litterantur` (rare). Currently rendered "alleged[?]". Resolve via PDF.

## Scholion (pp. 690)

The OCR for the SCHOLION on p. 690 is severely garbled (column overflow + half-tones bleeding). Numerous `[?]` flags throughout sections **I** and **II**. Section **III** (the *Auctores* list) is clean. Specific spots needing resolution:

- §I: `aliae quaestiones speciales [?]` — OCR `bleniiila spfui;ilia` is unrecoverable from OCR alone.
- §I: `m. 2[?]. a. 1–7[?]` — OCR has `m. "..` and `a. 1-7.1` — resolution: PDF.
- §I: `pro statu Auctoris[?]` — OCR `\iiitoris` could be `Auctoris` or `viatoris`. The latter (state of the *wayfarer*) is the standard scholastic phrase ("intellect for the *status viatoris* directly knows singulars"). Strong likelihood: `viatoris`. Marked `[?]` pending PDF confirmation.
- §II: `nisi per aliud[?]` — OCR clean enough but apparently a quotation from Thomas; `[?]` marks the boundary of the quoted vs paraphrased text.
- §II: All `[?]` flags after the second Bonav. self-citation (`II. Sent. d. 34. a. 2. q. 2`) until end of section II, including the apparently garbled `«sic[?] malum nullum[?] aliquod[?]…»`, `«quamvis malum non subsit divinae[?]…»`, and `[Reprobatio?]`. The OCR rows 27139–27145 are too damaged to reconstruct without PDF.
- §II: `(infra d. 46. q. 1. ad 3.[?])` — citation may be `q. J.` or `q. 1.` — strong likelihood `1.`.

Resolution path: extract pp. 689–690 at 600 dpi, transcribe the Scholion section directly from PDF, then come back and replace the `[?]` zones in §II.
