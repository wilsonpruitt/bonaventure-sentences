# Tier-2 ambiguities — bon-sent-I-d18-a1-q5

Date: 2026-05-02

1. **Scholion I, near end**: OCR fragment `quae in TOip. exponuntur` — read as *Resp.* (Responsione). Flagged `[?]`.
2. **Scholion I, conclusion of opinio 2**: OCR `Himm putal` — almost certainly `sed putat`. Flagged `[?]`.
3. **Scholion I, after first opinio's reasoning marker**: OCR `(ad t.)` rendered as `(ad 1.)` (likely "ad primum"). Silent fix (numeral 1 vs OCR `t`).
4. **Scholion II opening (top of p. 332)**: OCR is severely degraded — `S. lionii\niliini i\\plii-;il iiiri:i d. ... Ccleri aiiliqui ' iiicnli. — Qii ... II. CIV. Alc 1. i|. ()i. 111. :!. ...`. Reconstructed conjecturally as the bibliographic citation list of authors who treat the same opinion. The line "II. CIV. Alex. Hal." is a reading of `II.` plus `CIV.` (likely a garble; possibly reference numeral). Flagged `[?]` at multiple points and footnoted in apparatus entry [^31].
5. **Apparatus [^10] paulo infra cod. A `iliffert` / `differt`**: OCR `(Ulferl.` ambiguous. Read as `differt`. Flagged `[?]`.
6. **Apparatus [^9] tail "alii ut S. Bonac. — Tom. 1."**: This is in fact a page-footer artifact (running foot of vol. I). Preserved as-is in apparatus to flag for owner review.
7. **Body 4 (Contra), arg.4**: `quodsi ' spiritus sanctus et amor est proprietas` — assumed `quodsi` (= "but if"). The OCR superscript marker after `quodsi` (originally numbered `?`) was assigned to footnote on `quodsi` per Quaracchi marker spacing. Confidence high.
