# Tier-2 ambiguities — bon-sent-I-d4-a1-q2

Rebuilt from scratch 2026-05-10 from raw OCR lines 23974–24227 (d.1-d.10 rechunk pipeline). Supersedes 2026-05-08 entries.

## `[?]` flags inline

- **Respondeo, "de virtute sermonis"**: OCR shows `de virtute*` with footnote marker but ink-bleed; ambiguous whether printed `virtute` or `veritate`. Currently rendered `virtute` with `[?]` flag. Apparatus note 6 records the codex variant `cc veritate` (R), so reading `virtute` follows Vat. → Resolve via 600dpi PDF eyes-on at p.100 mid.
- **End of Respondeo §1**: OCR fragment `(1   verbum  simpli(  / ctionem` — broken across line break, suggesting `non sequitur ad verbum simpliciter distinctionem`. Currently rendered `[simpliciter? distin][?]ctionem`. → Resolve at p.100 bottom margin (running marginalia tag fragmenting the OCR).
- **Ad 3, "scilicet quod Deus [genuit?] Deum"**: OCR has bare `Deus` with no verb visible between `Deus` and `Deum` (lines 24180–24181). Likely elision of `genuit` (or `genuerit`) by typesetter; currently rendered `[genuit?][?]`. → 600dpi p.101 head check.
- **Scholion I, "ad suum subiectivum"**: OCR `subiectivum` is an unusual form; expected reading is `substantivum` (consistent with surrounding scholion logic about adjective/substantive). Currently rendered `subiectivum [substantivum?][?]`. → 600dpi p.101 footer.
- **Scholion I, "importat [alietatem?] circa suum substantivum"**: OCR shows hyphen-broken `impor- / circa` with `alietatem` not visible in the slice (lines 24214–24215). Inferred from context (the scholion's whole point is alietas); currently rendered `[alietatem?][?]`. → 600dpi p.101 confirms.
