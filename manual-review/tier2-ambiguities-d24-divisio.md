# Ambiguities log — bon-sent-I-d24-divisio

Source: `raw/bonaventure_vol1_pt2_raw.txt` lines 300–354 (printed p. 419, pdf p. 521).

## Inline `[?]` flags in chunk

None — no `[?]` flags placed inline. All OCR garbles in the divisio + tractatio body were resolvable from context.

## Silent OCR fixes (kept without flag)

- `DIVTSIO TEXTUS` → `DIVISIO TEXTUS` (raw line 300; T/I substitution).
- `signiflcetur`, `signiflcent`, `signiflcet` → `significetur`, `significent`, `significet` throughout (fl/fi ligature OCR).
- `nuraeralem` → `numeralem` (raw 309; ra/m garble).
- `secnnda` → `secunda` (raw 311; n/u).
- `distinetus` → `distinctus` (raw 311; n/c).
- `distincta.e` → `distinctae` (raw 312; period for ligature).
- `Cmn` → `Cum` (raw 313).
- `trimts` → `trinus` (raw 313; m/n + t/u OCR).
- `eorura` → `eorum` (raw 321; ra/m).
- `boc` → `hoc` (raw 324; b/h).
- `terraino` → `termino` (raw 327; rai/m).
- `noraen` → `nomen` (raw 325; ra/m).
- `posilive` → `positive` (raw 327; l/t).
- `relationera` → `relationem` (raw 329; ra/m).
- `Etquantum` → `Et quantum` (raw 324; missing space).
- `uniis` → `unus` (raw 325; ii/u).
- `complectente` retained (raw 322).

## Apparatus normalization (silent)

- `Gralianum` → `Gratianum` (l/t) — note 1 referent in left-column littera footnote (note 1 here is the right-column NOTAE AD COMMENTARIUM, which spelled cleanly in the OCR; this entry concerns the left-column littera apparatus and was not used).
- `irinus` → `trinus` (note 2, left-col littera apparatus — not used here).
- `cje-/neralium` line-break joined → `generalium` (note 1 NOTAE).
- `Imec` → `haec` (note 3 NOTAE; first letter OCR garble).
- `signiflcent` / `signiflcetur` / `signiflcet` → `significent` / `significetur` / `significet` (note 3).
- `distinciis` → `distinctis` (note 3; n/c).
- `Disl.` → `Dist.` (left-col littera footnote 3, not used).
- `subnexis propositionibus` retained.

## Marker placement decisions

- `[^1]` placed after `excepit` (raw line 303 left col; OCR shows `excepit '`).
- `[^2]` placed after `significetur` (raw line 309 left col; OCR shows `significelur ^` immediately before `per dictionem numeralem`); justified by NOTAE 2 referent ("additur *per nomen numerale sive*"), which describes a Vatican-edition addition at this position.
- `[^3]` placed after `distinctus` (raw line 311 right col; OCR shows `distinetus`); justified by NOTAE 3's first sentence ("Vat. perperam ... *distinctis*").
- `[^4]` placed after `termino` (raw line 327 left col); justified by NOTAE 4 ("Cod. bb *termino*").

## TRACTATIO question count

- Three principal questions enumerated (numerical+partitive *unus*; simply numerical *tres*; embracing both, *trinus*).
- Two sub-questions under the first (positive vs. privative; secundum substantiam vs. secundum relationem). This matches the d.24 ART. I structure (2 questions on numerical/partitive *unus*). Expected and observed: 2.
- ART. II and ART. III openers are NOT in this divisio (introduced separately at raw 821-858 and 1165-1176 per task brief).

## Items dropped per convention

- COMMENTARIUS IN DISTINCTIONEM XXIV. rubric (raw 291) and short opener "Hic diligenter inquiri oportet, cum in Trinitate non sit diversitas." (raw 296) — dropped per project convention for divisio chunks.
- Left-column littera footnotes 1–5 (raw 336–344) belong to the LITTERA TEXTUS at top of p. 419 (Lombard's text, raw 275–289), not to the divisio; they are handled in the d.24 littera chunk.
