# Vol VI — *Commentarius in Ecclesiasten*, CAPITULUM I SHAKEDOWN GATE (printed pp. 3–20)

Run 2026-09-20, after `bon-eccl-c1-v16-18` (`a9cda00`) closed Capitulum I. Ruling 6 placed this gate at p. 19;
Cap. I in fact closes at the head of p. 20, so the gate fires over **pp. 3–20**. ⛔ **Per ruling 6 as amended
(Wilson, 2026-09-20, `vol6-pilot-setup.md`) THIS GATE CARRIES NO DEPLOY** — there is exactly one deploy for this
work and it is at the end of Ecclesiastes, p. 103. Push stays cheap, separate and Wilson's per action.

**Procedure, the one the `perfectione` and `serm` gates established and this gate followed:** a read-only agent
compiled an evidence sheet FIRST — `manual-review/vol6-cap1-shakedown-evidence.md`, 611 lines — giving per docket
item the site count by chunk **over the live region**, the decisive quoted sentence in both languages, and the
realistic alternatives each with a one-line cost. **Wilson then ruled all six items in one sitting without opening
a chunk.** No builder settled anything.

**Scope: SEVEN chunks, pp. 3–20 with no gap, 156 numbered apparatus entries.**

| chunk | division / section | type | pages | entries |
|---|---|---|---|---|
| `bon-eccl-prooem` | 0 / 1 | prooemium | 3–8 | 55 |
| `bon-eccl-prol` | 0 / 2 | prologus | 8–9 | 2 |
| `bon-eccl-c1-v1` | 1 / 1 | pericope | 9–10 | 11 |
| `bon-eccl-c1-v2-7` | 1 / 2 | pericope | 10–15 | 49 |
| `bon-eccl-c1-v8-11` | 1 / 3 | pericope | 15–17 | 17 |
| `bon-eccl-c1-v12-15` | 1 / 4 | pericope | 17–19 | 15 |
| `bon-eccl-c1-v16-18` | 1 / 5 | pericope | 19–20 | 7 |
| **total** | | | **18 pp.** | **156** |

**Denominators.** Entry counts are parsed from every `[^p<page>-<n>]:` definition in each chunk's `## Apparatus`
(`tr-` labels excluded, as both checkers exclude them). **Every census in this log and in the evidence sheet is
taken over the BUILT FILES and over the RENDERED regions** — `## Latin`, `## English`, `## Apparatus`, with
frontmatter and `## Notes` stripped. ⛔ Not over `raw/`: Vol VI's raw is cascade-fragmented precisely in the
apparatus, which is where ruling 5 lives (the `bon-serm-s2` lesson, applied here from the start).

**Structural verification, derived from the files and not from the resume note:** pp. 3–20 contiguous (union of all
`printed_pages` = `range(3,21)` exactly) · every page owned exactly once with a contiguous 1..N register on all
eighteen leaves · `KNOWN_TOTALS_VOL6` matches the measured totals on all eighteen · all five Cap. I pericope spans
match the frozen record digit for digit. **No discrepancy anywhere in the structural record.**

---

## ▶▶ THE SIX RULINGS — WILSON, 2026-09-20. These are Vol VI's settled conventions; do not re-litigate them.

### (1) THE ITALIC CONVENTION — **p. 11 STANDS AS PRINTED; the exception is RECORDED, not repaired.**

p. 9 n. 4 states Quaracchi's inversion in Quaracchi's own words — *in expositione textus non potuimus retinere
solitum morem, verba Scripturae litteris italicis exprimendi; alioquin expositio non bene distingueretur a textu* —
and its scope is exactly where the inversion holds and exactly where it stops:

- **Prooemium pp. 3–8:** Scripture **italic**, 33 sites. The inversion does not reach it.
- **expositio:** **roman in guillemets**, and it holds on every leaf but one — p. 9 (3) · pp. 10/12/13 (≥29) ·
  p. 15 (7) · p. 16 (4) · p. 18 (17) · p. 19 (9) · p. 20 (2).
- **Quaestiones:** **italic, unguillemeted**, 9 sites; the inversion is simply not drawn there.
- **p. 11: ten italic runs inside the expositio**, where the rule predicts roman.

▶ **Ruled: leave as printed, record the exception.** 0 Latin lines, 0 English lines changed. ⛔ **Normalising was
refused on the standing rule that Quaracchi is never silently emended, and here the refusal is not merely formal:
five builders have measured the leaf and none has claimed a discriminator, so a normalisation would assert a
compositor's lapse that nobody has evidence for.**

⭐ **A candidate discriminator is DEAD, measured rather than theorised, and this gate records it so it is not
re-proposed:** "italic marks a self-quotation of Ecclesiastes" fails on p. 16, where `c1-v8-11` prints
*infra secundo:* « Futura tempora oblivione cuncta operient » — Ecclesiastes quoting itself, **roman, in
guillemets, in the expositio.**

⭐ **Same-leaf controls now number three, and p. 11 carries one of them:** Hugh of St Victor stands roman in
guillemets ×2 on p. 11 itself, and again on p. 17 (eight lines above an italic Jeremias) and p. 20. **Guillemets
are never abandoned; Scripture alone changes sides.** So p. 11's non-scriptural quotations obey the rule while its
scriptural ones do not — which is the exception at its sharpest, and still not a discriminator.

**Type: DECISIVE-SENTENCE question.** No count of roman sites — 72 or 700 — can decide whether p. 11 is a lapse or
an unstated sub-rule. It goes to the p. 103 work-close gate as a single-leaf exception to an otherwise unbroken
rule, with eleven further capitula of evidence by then.

⚠ **Two defects in the carried record, corrected here.** (i) **FIVE chunks have measured the inversion, not four** —
`c1-v1`'s three roman quotations on p. 9 were omitted from the docket's region list, and p. 9 was called a positive
zero when only `prol`'s upper third of that leaf is one. (ii) **pp. 10/12/13 have never been counted exactly**;
"~58 + 11 ≈ 69" carried a tilde through three hand-offs and is the only figure the record has ever held. The
mechanical floor for those three leaves is 29. **An exact count is owed at the p. 103 gate, not before.**

### (2) *magister* — **RATIFIED at n = 3 and TAKEN OFF THE DOCKET.**

| chunk | site | Latin | English |
|---|---|---|---|
| `c1-v1` | p. 10 n. 1 (Jerome) | *aequalis **magister** est* | "the **teacher** is an equal" |
| `c1-v8-11` | p. 15 body (II Tim. 4:3) | *coacervabunt sibi **magistros*** | "heap to themselves **teachers**" (Douay agrees; nil cost) |
| `c1-v12-15` | p. 19 n. 1 (Lombard) | *complectitur **Magister** in II. Sent.* | "the **Master**" — the corpus-wide exception |

**Two of the ruling, one of its named exception, and the exception is self-identifying** — it is fixed by its own
object (*in II. Sent. d. XXXVI. c. 1*), not by inference. ▶ **Same shape as *dominium* at n = 2: the count is what
decides.** ⭐ ***doctor* → "doctor", never "teacher"**, held deliberately so the two cannot converge.

⚠ **Correction to the carried record: *doctor* is NOT zero in Vol VI.** `bon-eccl-prooem` p. 5 n. 6 quotes the
Vulgate variant *volentes esse legis **doctores***, and the **En.** half repeats it untranslated. **The zero is true
of the English and false of the Latin** — so the next chunk that meets *doctor* in Bonaventure's own prose is the
second site, not the first, and the docket would have told it otherwise.

### (3) *doctrina* — **CONTEXTUAL UNDER THE DIKAISUNE RULE, with a `tr-doctrina` note. 0 text lines changed.**

⚠ **The docket said "3 sites, new". Measured over the built files: TEN Latin sites in FOUR chunks, taking THREE
English words**, chosen by three builders who did not know of each other's sites.

| chunk | sites | rendering | why |
|---|---|---|---|
| `prooem` | 4 body + 1 apparatus | **doctrine** | the fixed pair *scientia et doctrina* |
| `c1-v1` | 1 | **teaching** | *doctrinam parabolarum **tradidit*** — the act and its matter |
| `c1-v8-11` | 1 | **teaching** | *per inventionem et doctrinam*, paired with hearing against sight |
| `c1-v16-18` | 3 | **learning** | *atque doctrinam, **quantum ad cognoscenda*** |

**The decisive sentence is `prooem`'s** *et ideo de tali potest esse **scientia et doctrina*** ("and therefore of
such a thing there can be science and doctrine"), **because that one clause is what makes a single English word
impossible**: "learning" is unavailable at four sites where the pair is the settled scholastic term of art, and
"doctrine" is unavailable at Prov. 8:10, where the Douay's "knowledge" is itself blocked by *scientia* four lines
later (*Qui addit **scientiam** addit et dolorem*).

▶ **Ruled contextual, exactly as *vacare* was ruled 2026-09-15**: choose the English by context and footnote the
word, saying what the Latin holds together and which side the English took. **The three existing renderings all
stand; no printed text and no translation changed.**

⭐ **FOUR notes were written, not one — one per chunk that carries the word, at its first occurrence.** The evidence
sheet costed the option at "1 def + 1 anchor in `prooem`", and that is the cheaper reading of the frozen convention
(*at the first occurrence per chunk, and wherever the sense leans to the other side*). **The per-chunk reading was
taken on the ground Wilson had just ruled on under item (5): notes do not reach across chunks on the site, so a
reader of `c1-v16-18` would never see a note that stands in `prooem`.** That argument is what killed option D
there; it applies here unchanged. Cost: 4 defs + 4 English anchors, 0 text lines.

### (4) HEADING CLASSES — **THE FLATTENING IS KEPT. Level tracks FUNCTION (ruling 4) and is uncorrelated with both
class and size.**

21 headings tabulated over the live region in three typographic classes: (1) letter-spaced small capitals ·
(2) Quaracchi's roman-and-italic centred structural labels (`PARS I.`, `ART. 2.`) · (3) letter-spaced roman
upper-and-lower-case sentence-form headings.

**The decisive evidence is the p. 10 PAIR:** `SEQUITUR HUIUS LIBRI TRACTATUS.` (27 px, `###`) and `PROPOSITIO REI
PROBANDAE.` (27 px, `####`) stand **on one leaf, in one column, in one typographic class, at one size, three lines
apart, and take different markdown levels.** ⛔ **No typographic measurement can separate them. Only function can** —
which is what ruling 4 says, and why four size-based challenges have each inverted (p. 9: 25 v 25 · p. 10: 27 v 27 ·
`c1-v8-11`: 28 v 26 · `c1-v12-15`: 28 v 26, the `####` taller than the `###` every time both stand on a leaf).

⚠ **The docket's premise was wrong and is corrected here: the three classes are NOT all flattened to `####`.**
Class 1 takes both levels (`###` for `Capitulum I.`, `Quaestiones.`, `Expositio huius prologi.`; `####` for
`De titulo.`, `Propositio rei probandae.`, `Divisio totius probationis.`); class 2 takes both (`Quaestio I–IV` at
`###`, `Pars I.`/`Art. 1.`/`Art. 2.` at `####`); **only class 3 is uniformly `####`**, and class 3 itself carries
two sizes (28 and 21 px, both on p. 17).

▶ **Deepening to `#####`/`######` by nest depth was refused now** (~14 heading lines × 2 languages, per-site, and it
needs a depth model that does not exist and that `build-content.mjs` has never been asked to render). **Revisit at
p. 103, when twelve capitula have been seen and the deepest nest in the work is actually known.** ⚠ Check every
heading at ≥1.7× and **never bound-box a `Q`** — its tail adds 6–8 px.

### (5) THE `tr-` MECHANISM — **EXTENDED. An English-side anchor inside `## Apparatus` now satisfies the pairing,
for `tr-` labels only.**

`c1-v12-15` p. 18 n. 1 quotes the Vatican edition's addition: *…nec **vacare** etiam **ad hoc** potest qui non habet
pacem et concordiam* → "nor can he even be **free for** this who has not peace and concord." **The site was already
translated correctly and consistently** — `ad` + accusative, the "free *for*" construction, inside the scope of the
`[^tr-vacare]` note `c1-v8-11` already carries. ⛔ **But the ratified dikaisune ruling requires a translator's note,
and the note could not be attached**: the word stands in an apparatus entry's **En.** half, and `## Apparatus` is
outside the English region both checkers test.

**The gap, stated exactly:** `tr-` was exempted from the LATIN anchor requirement (and fails if anchored there) but
**was never exempted from the ENGLISH one**, so a note anchored beside the word it is about would have fired
`PAIR: defs not anchored in English` — in a scan that is otherwise zero in vol6 across 2,126 chunks. **A real gap in
the mechanism, not a builder's lapse.**

▶ **Ruled: extend the mechanism.** Applied 2026-09-20:
- `tools/polish-style-scan.py` — `## Apparatus` markers are now collected, and a `tr-` def anchored there is
  exempt from `missing_eng`. Quaracchi's numbered entries are untouched and still require both anchors.
- `tools/check-vol5-apparatus.py` — the same carve-out in the `TRANSLATOR'S NOTE pairing` test; apparatus-region
  anchors (def lines excluded) now count as English-side anchors for `tr-` labels.
- `vol6/bon-eccl-c1-v12-15.md` — `[^tr-vacare]` added, anchored in the entry's **En.** half.

⭐ **Tested positive and negative**, as the 2026-09-15 mechanism was: with the anchor where it stands, the OLD rule
flags `['tr-vacare']` and the NEW rule flags `[]`; nothing else in 2,126 chunks moved. **The renderer needed no
change** — `renderInline` is already applied to an entry's `en` half, so the marker resolves to a `sup` linking to
the Translator's Notes block, verified by reading `text-reader.tsx`.

**Type: DECISIVE-SENTENCE question**, and the decisive sentence is that apparatus entry: *ad hoc* is what puts the
site inside `tr-vacare`'s existing scope rather than opening a fourth construction. **n is irrelevant — one site in
an unreachable position was the whole item.**

### (6) THE RESOLVER DOCKET — **RECORDED, NOT SCHEDULED. All five artefacts confirmed live; none is fixed.**

| # | artefact | status |
|---|---|---|
| 1 | `c1-v1` p. 10 n. 5 `supra pag. 8, nota 5` → `page-multi` though the label `p8-5` is unique in the corpus | live, **coarse** |
| 2a | `c1-v2-7` p. 11 n. 13 `ibid. v. 5. et 6.` carried by **cross-note anaphora** to `bon-eccl-prooem` | live, **wrong**, looks resolved |
| 2b | `c1-v16-18` p. 19 n. 5 `ibid. 31, 1.` filed **`Eccli 47:15` at confidence A** when it is **Prov. 31:1** | live, **wrong**, filed confident |
| 3 | `c1-v8-11` p. 17 n. 5 `ed. Serrani tom. III. pag. 39` — a named FOREIGN edition's tome read as Quaracchi's | live, **coarse** |
| 4 | `c1-v12-15` p. 19 n. 3 `cfr. tom. II. pag. 655, nota 3` — that class's MIRROR, and **correctly** resolved | the control |
| 5 | the lemma shorthand `Cap. N, M` — **28 occurrences, 0 ledger records**, invisible by accident | live |

⭐ **2a and 2b are one missing rule: an apparatus note's `ibid.` takes its antecedent from WITHIN the note, never
from the preceding note.** ⭐ **#4 is why #3 cannot be fixed by suppressing bare `tom. N. pag. N`** — one of the two
shapes is right; the discriminator the resolver lacks is the `ed. <name>` standing before `tom.`, and the token
"Serrani" never reaches the ledger at all.

▶ **Ruled: record now, schedule the docket as its own job later.** It is tool work with **zero chunk lines**, but
proving no collateral movement needs a full 23,957-record before/after diff — 2a/2b alone move every `anaphor->`
record in the corpus (2,033 within-chunk + 89 cross-chunk). **That is its own session, not gate work and not deploy
work.** ⛔ **Corpus QA is 206 and NONE of it is ours** — the 206 is the Vol V unmasking that followed
`VOLUME_COMPLETE[5]` going True, verified unchanged.

---

## ▶▶ THE POSITIVE ZEROS — each CHECKED over the built files, not assumed. **Three of six did not survive.**

| claim | measured | verdict |
|---|---|---|
| *lumen* zero across all seven chunks | **ZERO.** 3 `lumin-` hits, all *luminaria* (Gen. 1:16) | ✅ **CONFIRMED** — ruling 2 is ONE-SIDED throughout (*lux* → "light" only) and **no `tr-lumen` is owed in this volume** |
| the *flumen* trap | a bare substring sweep returns **26 hits in `c1-v2-7` alone**, nearly all *flumina* (Eccl. 1:7), plus *flumen* in `c1-v16-18` | ✅ **TRAP LIVE. Always use `\b`** |
| ruling 5 / `fundam.` not reached | **ZERO in all 156 apparatus entries.** The 2 rendered `fundam-` hits are Latin BODY words (*fundamentum* p. 3; Prov. 8:29 *fundamenta* p. 12) | ✅ **CONFIRMED**, and censused over the built apparatus where `fundam.` lives — the `bon-serm-s2` lesson applied |
| *contuit-* zero · *pietas* zero | **ZERO** in all seven (every hit is `## Notes` discussing the zero) | ✅ **CONFIRMED** |
| bare English "intellect" zero | **ZERO** | ✅ **CONFIRMED** |
| *intelligentia* zero | **n = 1** — `prooem` *ad **intelligentiam** praedictorum* → "for the understanding of the foregoing" | ⛔ **NOT a zero.** Precedent-covered by `bon-qsc-q2` (the ordinary idiom, explicitly not a departure from *intelligentia* → "intelligence", which governs the term of art) |
| *intellectus* met twice, both → "understanding" | **n = 3** (`c1-v8-11` ×2, `c1-v12-15` ×1), all three → "understanding" | ⚠ **count off by one; the ruling holds 3/3** |
| *doctor* zero | **n = 1 Latin, 0 English** | ⛔ **NOT a zero** — see ruling (2) |

⭐ **Why this matters, and it is the gate's transferable finding:** the three zeros that failed all failed
**quietly**, and in a hand-off note a checked zero and an unchecked one are the same sentence. None is a defect in
a chunk. **All three are defects in a carried summary** — which is exactly what CLAUDE.md says to expect of the
least-quotable-but-most-quoted lines in a `## Notes` block.

---

## ▶▶ WHAT THE GATE CHANGED

**Text: nothing.** No Latin line and no English rendering was altered anywhere in the live region. Rulings (1),
(2), (4) and (6) changed nothing by design; (3) and (5) added notes beside text that was already right.

| file | change |
|---|---|
| `vol6/bon-eccl-prooem.md` | `[^tr-doctrina]` def + English anchor |
| `vol6/bon-eccl-c1-v1.md` | `[^tr-doctrina]` def + English anchor |
| `vol6/bon-eccl-c1-v8-11.md` | `[^tr-doctrina]` def + English anchor |
| `vol6/bon-eccl-c1-v16-18.md` | `[^tr-doctrina]` def + English anchor |
| `vol6/bon-eccl-c1-v12-15.md` | `[^tr-vacare]` def + anchor in the entry's **En.** half |
| `tools/polish-style-scan.py` | `tr-` exempt from `missing_eng` when anchored in `## Apparatus` |
| `tools/check-vol5-apparatus.py` | same carve-out in the `tr-` pairing test |
| `vol6/bon-eccl-c1-v2-7.md` | Notes: "ten" corrected against its own enumeration of nine |

⚠⚠ **ONE FROZEN CLAIM IN CLAUDE.md IS FALSE AND IS CORRECTED BY THIS GATE.** The `tr-` convention says
*"`build-citations.py` reads only `**La.**` halves, so notes never enter the index."* **They do.** A translator's
note that NAMES a scripture reference in its English prose is parsed like any apparatus text: `bon-serm-s3`'s
`tr-lumen` already contributes **three** ledger records (Ps 106, Ps 37, Ps 4) and `bon-serm-tract`'s one (Rom 13),
all pre-existing and all at confidence C, chapter level. This gate's `c1-v16-18` `tr-doctrina` adds a fifth
(Prov 8), which is the +1 in the ledger below. **Not a regression and not a defect** — the records are correct
about what the note says — **but it means our own editorial voice is indexed alongside Quaracchi's, which nobody
decided.** ▶ **Recorded for the p. 103 work-close gate; not settled here, because it is corpus-wide and touches
four deployed Vol V records.**

---

## ▶▶ SUITE AT THE GATE, WITH DENOMINATORS

| tool | counted | verdict |
|---|---|---|
| `check-vol5-apparatus.py --volume=vol6` | **7 chunks, 156 entries, 18 page totals**; La = En per chunk | **all passed**; p. 20 `PENDING n.2–8` (Cap. II's, correctly unwritten) |
| `check-vol5-census.py` | vol5 **167/167** · vol6 **7/7**; vol6 **12 runovers (11 gutter, 1 page: p. 16 n. 7)** | **rosters agree** |
| `polish-style-scan.py` | **2,126 chunks, vol1–vol6 in scope** | 11 issues / 6 chunks — 10 PAIR (Vol III/IV Sentences), 1 V5LABEL (`bon-hex-c23`). **ZERO ours** |
| `check-live-flags.py vol6` (bare positional) | **7 chunks** | **no live `[?]` flags** |
| `build-citations.py` | **2,126 chunks → 23,957 records** (was 23,956); vol6 **212** (was 211) | **QA 206, unchanged; none ours.** The +1 is the `tr-doctrina` Prov 8 record above |
| `build-content.mjs` | **15 books, 2,125 questions** | **2,125 translated**, unchanged |

**Entry total is unchanged at 156**: both checkers exclude `tr-` labels from entry counts, `KNOWN_TOTALS` and page
ownership, exactly as the 2026-09-15 mechanism intended. Five translator's notes now stand in the volume
(4 × `tr-doctrina`, 1 × `tr-vacare`) beside `c1-v8-11`'s existing `tr-vacare`.

---

## ▶▶ WHAT REMAINS OPEN AFTER THIS GATE

1. **p. 11's italic exception** — recorded, no discriminator, to the p. 103 work-close gate.
2. **An exact expositio count for pp. 10/12/13** — owed at p. 103; "~58 + 11 ≈ 69" is not a measurement.
3. **The heading nest** — flattening kept; revisit at p. 103 with twelve capitula in view.
4. **The resolver docket** — five Vol VI artefacts + the Vol V carry-overs, its own scoped job, unscheduled.
5. **Translator's notes entering the citation ledger** — new, corpus-wide, for p. 103.
6. ⬜ **The p. 103 deploy-only batch** is unchanged and is listed in `next-session-resume.md`.

▶ **THE FRONT AFTER THIS GATE is the FIRST PERICOPE OF CAPITULUM II, opening MID-LEAF on p. 20**
(`division: 2`, `section: 1`, `type: pericope`). Its verse range is **not yet read** — take it off the index plate
(PDF 677–678). **It inherits p. 20 nn. 2–8.** ⚠ `CAPITULUM II.` is a full-width display heading, so `colcrop`'s
body-row window will be fought by it on that leaf.

⛔ Committed locally. **NOT pushed, NOT deployed** — each Wilson's own per-action OK.
