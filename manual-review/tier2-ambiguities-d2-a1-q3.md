# Tier-2 ambiguities — d.2 a.1 q.3

Rebuild from scratch 2026-05-10 (raw lines 17109–17359).

- **scholion II, citation list, "Scot., hic q. 5[?] et 7"**: OCR has `S.  et  7.` where `S.` could be `5.` or `8.` (cf. d.2 q.II scholion II which gave `q. 4. 7`). Currently rendered `q. 5[?] et 7`. → Resolve via 600dpi PDF eyes-on of p.56.
- **apparatus [^11], "pra qualibet[?]"**: OCR `pT&  qualibet` — clearly `pro qualibet` corrupted; `pra` is OCR garble for `pro`. Currently rendered `pra qualibet[?]`. → Resolve to `pro qualibet`.
- **apparatus [^14], "circa flnem rei[?]"**: OCR `circa  flnem  re-` then line break to `i  ex  mss.` — likely `circa finem rei` or `circa finem responsi`. Currently rendered `circa finem rei[?]`. → Resolve via 600dpi PDF.
