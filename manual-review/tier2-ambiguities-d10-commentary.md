# Tier-2 ambiguities — d.10 commentary preamble

## Resolution log

**bon-sent-I-d10-commentary, Lombard incipit lemma (raw line 38251)**: OCR ends the lemma snippet with the truncated fragment `ak-`. The full Lombard text continues with a verb beginning `ag-` (Quaracchi prints incipit lemmas as a brief incipit followed by `etc.`); the OCR `ak-` is almost certainly a misread of `ag-` (the standard reading in Lombard's *Sent.* I, d. 10, c. 1: *Nunc post Filii aeternitatem de Spiritu sancto, quantum Deo donante, aggrediar*). Currently rendered with `[?]` flag preserving the OCR fragment. → Resolve at next 600 dpi PDF eyes-on pass for d.1–d.10 polish; if PDF confirms `ag-` change to `aggrediar` (or `ag-` truncation matching the printed `etc.` cutoff). ACCEPT-FLAG until then.
