# Tier-2 ambiguities — d17 p2 a1 q3

One `[?]` flag in the chunk:

**bon-sent-I-d17-p2-a1-q3, Apparatus [^18] (corresponds to body marker after "ut supra probatum est" in Respondeo)**: PDF source-block reads "In corp. praeced. ii." — the OCR garbles the section indicator. "ii." may be "n. [number]" (which numbered note in the body of the preceding question being referenced) or "n." abbreviation followed by an Arabic numeral mangled by OCR. Currently rendered with `[?]` placeholder. → Resolve by visual check on a higher-dpi render of p.314 footnote 13 ("In corp. praeced.") to determine the precise section/numeral citation.

No other `[?]` flags. All other OCR garbles in the body (e.g. `Tertio qiiaferitur` → `Tertio quaeritur`, `oinne` → `omne`, `hominem` correct, `iniiniit` → `minuit`, `tdllitur` → `tollitur`, `Pmeterea` → `Praeterea`, `qubd` → `quod`) are unambiguous OCR artifacts silently corrected per CLAUDE.md guidance.

The OCR title-line reads `Utrmn caritas possit diminui.` — silent fix to `Utrum caritas possit diminui.` (standard scholastic question opener).
