# Tier-2 ambiguities — d.23 divisio

**bon-sent-I-d23-divisio, chunk boundary (raw lines 69438–69443, COMMENTARIUS opener)**: Raw OCR contains a `COMMENTARIUS IN DISTINCTIONEM XXIII.` rubric block with subtitle `De substantialibus nominibus divinis in speciali.` and Lombard-text opener `Praedictis adiiciendum est, quod cum omnia nomina, quae secundum substantiam` etc., between the d.22 dubia (or littera) end and the `DIVISIO TEXTUS.` heading at 69443. Per the d.20–d.22 chunking convention, this rubric+opener block is dropped from the divisio chunk; `line_start` set at 69438 (covering the rubric block but body begins at DIVISIO TEXTUS). Title rendered as commentary subtitle. → Resolved per d.22 convention.

**bon-sent-I-d23-divisio, NOTAE block sourcing (raw lines 69524–69546)**: Raw OCR shows footnote entries 1–8 in a block at lines 69524–69546. Notes 1–5 reference items in the divisio body (`de eadem`, `invicem`, `his`, `illud`, `Nam quaelibet`); note 6 references `debuerit ibi` which appears in BOTH the TRACTATIO QUAESTIONUM list (line 69498, marked `^`) AND the ART. I Q. I body (line 69514, marked `'`). The TRACTATIO occurrence carries the OCR superscript `^`, so note 6 is anchored in the TRACTATIO and assigned to the divisio chunk. Notes 7 and 8 (about `particula ibi` insertion + `Quod autem` vs `Et quod`, and `Hic, c. I. in fine` for Augustine `in littera`) belong to ART. I Q. I and are NOT included here. → Resolved.

**bon-sent-I-d23-divisio, divisio body OCR garbles silently corrected**: `egil → egit` (69446), `noniinum → nominum` (69447), `seyitA → sexta` (69453), `distinclione → distinctione` (69467), `Prinia → Prima` (69470), `huiusexceptionisinvestigat → huius exceptionis investigat` (69472), `Ideooritur → Ideo oritur` (69472), `Scd → Sed` (69475), `hie → hic` (69475), `quarla → quarta` (69477), `Jam → Iam` (69478), `sufficientei' → sufficienter` (69478), `utputo → ut puto` (69478), `ton-tinet → continet` (69480, line-break), `QUAESTIONnVI → QUAESTIONUM` (69484), `quaeest → quae est` (69459), `diciturde → dicitur de` (69460), `tribussingulariter → tribus singulariter` (69460). All standard OCR letter substitutions (n/u, l/i, e/c, broken-word) per CLAUDE.md cleanup rules.

**bon-sent-I-d23-divisio, COMMENTARIUS rubric subtitle OCR (line 69440)**: OCR rendered `De  substantialibiis  nominibus  diviiiis  in  speciali.` with `substantialibiis` (extra `i`) and `diviiiis` (n→i triple substitution). Reconstructed silently as `De substantialibus nominibus divinis in speciali.` for chunk title. → Resolved.

**bon-sent-I-d23-divisio, Lombard opener OCR (line 69441)**: OCR rendered `Pmedictk  adiiciendum  est,  quod  cum  omnia  nomina,  quae  secundum  subslantiam  etc.` — `Pmedictk` for `Praedictis` and `subslantiam` for `substantiam`. Not rendered in chunk body (per convention) but cited in resolved form in the chunk-boundary note above. → Resolved.

**bon-sent-I-d23-divisio, fn 5 anchor placement (raw line 69480–69481)**: OCR `quae  ton-/tinet  ^` — the `^` superscript follows the line-broken word `continet`. Anchor placed at end of `continet` per OCR. → Resolved.
