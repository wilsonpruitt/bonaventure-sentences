# d.11–d.20 Pass 2 — Style/Formatting Audit (full corpus)

**Date:** 2026-06-05. Decade-polish gate, Pass 2 of 3 (programmatic, no PDF reads).
Generated by `/tmp/pass2_full_audit.py` (all 8 polish-gate checks) + manual disposition.

## Scope

Tier-2 chunks scanned (identified by `transcription_status`):
**Vol I = 406, Vol II = 448, Vol III = 205 → total 1,059 Tier-2 chunks**;
217 skeletons / `distinctio: 0` prolegomena skipped. Special attention to the
fresh d.15–d.20 chunks and re-confirmation of d.11–d.14.

## Per-check results

1. **Required Tier-2 frontmatter** (`title_la`, `title_en`, `printed_pages`,
   `pdf_pages`, `source`, `has_apparatus`, `transcription_status`): **PASS** —
   0 chunks missing any required field across all three volumes.
2. **Standard structure** (`## Latin`, `## English`, `## Apparatus`): **PASS** —
   0 chunks missing a required section (Apparatus required only where
   `has_apparatus: true`).
3. **`transcription_status` prefix** `Phase C Tier 2 complete —`: **PASS** —
   0 prefix-drift among Tier-2 chunks.
4. **Apparatus marker pairing**: **flags found → 5 chunks FIXED, 4 chunks ACCEPTED**
   (some chunks carry both kinds; see below). After fixes, 0 unresolved
   mismatches.
5. **Page-break presence** (multi-page chunks have `<!-- page N -->`): **PASS** —
   0 multi-page chunks missing page-break comments.
6. **★ Scholion ordering** (body-first, `### Scholion` last in each language
   block): **PASS** — 0 violations corpus-wide. No chunk places a scholion
   ahead of its body (the d18-a1-q3 / commit 57fe0f0 class of bug does not
   recur anywhere in vol1/vol2/vol3).
7. **Legacy auto-chunked duplicates** (e.g. `d{N}-divisio.md` superseded by
   `d{N}-p1-divisio.md` + `d{N}-p2-divisio.md`; stray skeleton beside a promoted
   sibling): **PASS** — 0 duplicates. (The in-repo `tools/audit-style-formatting.py`
   dup regex matches only `I|II`; the Vol-III prefix `bon-sent-III-` was added in
   this one-off and found none. Flagged for a future tooling fix to the in-repo
   script.)
8. **`hasTranslation` build sanity**: **PASS** — build reports **1082 translated**
   (unchanged from pre-audit state). All edited chunks parse with non-empty
   bodies; no expected-translated chunk parses empty.

## Check #4 detail — apparatus marker pairing

### FIXED (5 chunks) — mechanical: marker present in one body but missing/mis-slugged in the other; parallel position unambiguous

- **`bon-sent-III-d15-a2-q1.md`** — `[^p337-1]` present in Latin (*Sed contra* 1,
  `…qui est ignorantia`) but absent in English. Added to English parallel
  (`…that defect which is ignorance[^p337-1].`).
- **`bon-sent-III-d17-divisio.md`** — `[^p363-2]` present in Latin
  (`quaestionem propositam`) but absent in English. Added to English parallel
  (`the proposed question[^p363-2] still more diligently`).
- **`bon-sent-III-d20-a1-q5.md`** — `[^p428-4]` present in Latin (`nisi libero`)
  but absent in English. Added to English parallel (`by the free[^p428-4]
  choice of the will`).
- **`bon-sent-III-d5-a2-q5.md`** — `[^2d]` present in Latin (`quod nomen`) but
  absent in English. Added to English parallel (`the name[^2d] *humanity*`).
- **`bon-sent-III-d7-dubia.md`** — body marker on *notiori* was mis-slugged
  `[^7]` (colliding with the p.182 received Hesychius note) in BOTH bodies,
  leaving the p.183 note `[^7b]` orphaned. Per the chunk's own `## Notes`
  ("note 7 … disambiguated **`[^7b]`** … keys *notiori⁷*"), corrected the
  *notiori* anchor to `[^7b]` in both Latin and English. Resolves both the
  orphan-def AND the `[^7]` double-meaning.

### ACCEPTED (4 chunks) — orphan apparatus defs with NO body anchor BY DESIGN (documented in each chunk's `## Notes`; Quaracchi editorial / cross-reference / scholion-source notes keyed to print line-numbers, not inline superscripts)

- **`bon-sent-III-d15-divisio.md`** — `[^notae-1]`, `[^notae-2]`: the two
  *Notae ad Commentarium* sub-block entries; editorial notes on the commentary,
  keyed to commentary line numbers in print, no inline superscript.
- **`bon-sent-III-d19-dubia.md`** — `[^p412-4]`, `[^p412-5]`, `[^p413-5]`,
  `[^p413-10]`: Master's-text / cross-reference / variant notes; the chunk's
  Notes block explicitly records "footers 4 and 5 … rendered with no body
  anchor per Quaracchi" (p.412) and "footers 5 and 10 are cross-reference/variant
  notes with no body anchor" (p.413).
- **`bon-sent-III-d5-a2-q4.md`** — `[^11]`: the standalone SCHOLION
  source-reference list (*Alex. Hal. … Biel*), printed with a marginal
  Roman-numeral sigil and no body marker; appended with an explicit
  "(Scholion source-reference — no body marker.)" lead per the chunk's Notes.

These are not defects — they faithfully render Quaracchi apparatus that has no
inline anchor. Recorded here so they are not re-flagged in future passes.

## Check #6 detail — scholion ordering

Every chunk whose `## Latin` or `## English` block contains `### Scholion` was
checked: in all cases `### Scholion` is the LAST subsection of its block and the
preceding body is substantial (>40 chars after whitespace-normalization). **No
violations.** The parser (`extractLanguageBlock`, build-content.mjs) takes
everything before `\n### Scholion\n` as body and everything after as scholion;
no chunk would parse to an empty body.

## d.11–d.14 re-confirmation + d.15–d.20 fresh-build check

- **d.11–d.14** (prior sessions): all chunks PASS checks 1–7; no regressions.
- **d.15–d.20** (this build front): 3 of the 5 marker-pairing fixes land here
  (d15-a2-q1, d17-divisio, d20-a1-q5) plus the accepted documented-orphans
  (d15-divisio, d19-dubia). All now clean. (The d5/d7 fixes are Vol III but
  outside the decade; fixed in the same pass since the full corpus was in scope.)
- Carried items from the resume note (p.426 footer-marker 6 skipped in source
  for d20-a1-q4; p.409 nn.1–5 backfill in d19-a2-q1; d18 backfills) are
  boundary-integrity items for Pass 3, not style-audit items; no style/format
  defect observed in those chunks here.

## Build verification

`cd site && node scripts/build-content.mjs` → **1082 translated** (unchanged;
not regressed). All edited chunks parse with clean marker pairing and non-empty
bodies.

## Result

Pass 2 CLOSED. 5 chunks edited (mechanical marker fixes), 4 chunks' orphan defs
accepted as documented, all other checks clean across the full 1,059-chunk
Tier-2 corpus.
