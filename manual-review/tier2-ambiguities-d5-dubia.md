# d.5-dubia Tier-2 ambiguity log

## [?] flags from full-rebuild pass (2026-05-08)

- **dub II respondeo, OCR line 27083 area**: chunk renders Bonaventure's gloss `bene dicitur homo esse ab homine` verbatim from OCR, but the construction (`homo esse ab homine`, infinitive subject + ablative agent) is unidiomatic-looking; Quaracchi footer note 4 (OCR 27154) supplies *se habendi* and notes that some codices read `Homo est ab homine` (i.e. finite verb). I have left the OCR-attested infinitive form and flagged with `[?]` for PDF eyes-on at next decade-polish pass. → Resolve at d.40 polish gate by checking 600dpi PDF p.120 around line 27083 plus the supra d. 1 dub. 1 cross-reference.

- **dub VII respondeo end, OCR line 27288**: OCR runs `et hoc patet per` and then jumps to `DUB. VIII.` heading at line 27291 — i.e., the antecedent of `per` is missing. Quaracchi footer 22 (OCR 27341-27343) explicitly notes that the Vatican edition added `et per hoc patet sequens` here `sed minus bene` (i.e. judged inferior). The Quaracchi text therefore deliberately leaves the sentence dangling at `per`. Body retains `[?]` flag in both Latin and English to signal this is an authentic textual irregularity, not an OCR/transcription gap. → Resolve at d.40 polish gate by confirming printed page 121 ends at `per` with no continuation visible.
