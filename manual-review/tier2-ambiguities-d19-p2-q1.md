# Tier-2 ambiguities — d19 p2 a1 q1

**chunk-id, scholion II opening line ("dub. 1.[?]")**: Quaracchi running head on PDF p.459 (printed p.357) prints `DIST. XIX. P. II. ART. UNICUS QU.\EST. II.` above the SCHOLION block, but the scholion belongs to QUAESTIO I (the question whose body ends on p.356). Treated as a print error; left scholion attached to q1 per content. → No translation impact.

**chunk-id, scholion III "Report. hic q. S."**: OCR reads `q. S.` (capital S). Almost certainly `q. 3.` — the `S` is OCR garble of `3`. Rendered as `q. 3.[?]`.

**chunk-id, footnote-position alignment**: Apparatus entries 1–23 are present and internally consistent with body markers [^1]–[^23] in both Latin and English, but the mapping between specific body positions and Quaracchi's per-page printed footnote numbers (8 fns p.355 + 15 fns p.356) was approximated based on OCR marker positions. A few markers may be off by one position relative to the printed edition (especially the cluster around the Respondeo / "perfectum" / "secundo" / "integritas" markers on p.356). Content of each entry is verbatim from OCR. → Worth a verification pass against the PDF if rigorously checking footnote positions; semantic content is preserved.
