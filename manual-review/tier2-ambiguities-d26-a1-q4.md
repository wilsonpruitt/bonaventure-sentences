# d.26 a1 q4 — Tier-2 ambiguities

(Generated 2026-05-04 alongside Tier-2 promotion of `vol1/bon-sent-I-d26-a1-q4.md`.)

No genuine ambiguities required `[?]` flags in the chunk body. All Latin OCR garbles were resolved unambiguously by context against the silent-fix list in CLAUDE.md and by cross-checking two-column reflow against page-break markers and marginalia anchors.

Notes on judgement calls (transparent, not flagged inline):

- Two-column OCR reflow: the OCR interleaves col A and col B line by line. Reflowed col A first (lines 4178–4237 page 460; lines 4261–4308 page 461) then col B. The `<!-- page 461 -->` boundary is placed at the column-A column-break ("ergo omnis | notio est proprietas"), matching the printed-page reflowed sequence; col B of page 460 (CONCLUSIO + first half of respondeo through "et hinc est") sits before the `<!-- page 461 -->` marker, and col B of page 461 (1.2.3 / *4.* / *5.* / *6.* responses) sits after. This preserves the printed-page running heads even though the per-page narrative is split across both column flows.
- Page 460 carries footnote sequences in BOTH columns (col A footers numbered 1–4 plus a continuation; col B footers 5/—/'/3 with various OCR glyphs). Apparatus has been renumbered consecutively per the project convention. Result: 14 entries.
- The orphan col-B footer "Cfr. supra pag. 152, nota 1" (line 4254) is folded into entry [^6] alongside the parallel "Cfr. supra pag. 132, nota 1" (line 4249) — both are cross-references attaching to "ratio cognoscendi" / "nomen notionis est in plus".
- Apparatus entry [^8] folds two notes that printed as separate footers (a brief Vat./ed. 1 variant note and the longer Albert/Gilbert/Simon-of-Tournai cross-reference for "tres opiniones") since they both attach to the same "Intelligendum igitur" anchor in the body.
- Silent OCR corrections: `qnaeritur → quaeritur`; `Qnod → Quod`; `secnndum → secundum`; `cmnes → omnes`; `comparanlnr → comparantur`; `nnmero → numero`; `indefmito → indefinito`; `siniplicius → simplicius`; `sinipli-cissinium → simplicissimum`; `infiniti respectus`; `Damascenuni → Damascenum`; `generationera → generationem`; `ralione → ratione`; `pidprietas → proprietas`; `proiuirlas → proprietas`; `persiniae → personae`; `flat → fiat`; `proprietatura → proprietatum`; `obiicilur / obiicitnr → obiicitur`; `inflnitas → infinitas`; `ralione → ratione`; `boc → hoc`; `producti → producti`; `dieit → dicit`; `simihter → similiter`; `seqnitur → sequitur`; `subieclo → subiecto`; `accidenlis → accidentis`; `relaiio → relatio`; `relaliones → relationes`; `proprietds → proprietas`; `noiio → notio`; `nolio → notio`; `proprietales → proprietates`; `Quot. → Quot`; `idtimum → ultimum`; `ventate → veritate`; `aUum → alium`; `cognoscendi *` (asterisk = footnote anchor); `paternalibus`; `inilescunt → innotescunt`; `inilotescere → innotescere`; `inilescere`; `inflnitas → infinitas`; `quinarium`; `appropinquavit`. All silent corrections fall within the CLAUDE.md letter-substitution rule for unambiguous OCR garbles.
- Marginalia tags stripped from running text: `Ad opposi-tum.`, `Fundamenta.`, `Determ. quaest.` / `Detarmiu. quaest.` (line 4180 right margin "Dei,ermiD:"), `Conclusio 1.`, `responsio i`, `conciusio etiam`, `Conclusio principalis.`, `Solutio op-positorum.`, `Iudicium de o-pinionibus.`, `5a solutio.`. These are editorial Quaracchi side-glosses, not part of Bonaventure's text.
- Scholion: the OCR truncates `SCHOLIOK` (line 4311) — read as `SCHOLION`. Two scholion sections (I, II) confirmed against the column-reflowed text; col A holds the start of I and ends of I, col B holds the rest of I and all of II. The bibliographic list in II contains Quaracchi cross-references that have been preserved verbatim (with minor OCR garbles silently corrected, e.g. `m.` / `q.` formatting).
